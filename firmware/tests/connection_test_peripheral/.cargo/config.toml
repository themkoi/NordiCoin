[target.'cfg(all(target_arch = "arm", target_os = "none"))']
runner = "probe-rs run --chip nRF52805_xxAA"

[build]
target = "thumbv7em-none-eabi"

[env]
DEFMT_LOG = "info"
TROUBLE_HOST_CLIENT_ATT_TABLE_SIZE = "64"
